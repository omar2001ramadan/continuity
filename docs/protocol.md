# TSL Protocol Notes

The protocol reference implementation treats signed objects, canonical serialization, Merkle inclusion proofs, revocation checks, and checkpoint settlement as the canonical product surface. Hosted APIs are convenience wrappers around portable proof data.

Current canonical implementation: `packages/core-ts`.

