# Delegation Authorization v0

Status: RC4 deterministic profile.

Agent action verification uses `agent_action.issued_at`, validates policy signatures and chain roots, applies deny-overrides-allow, checks revocation pointers, enforces action/resource scope, and applies value, tool, currency, counterparty, rate, human approval, and parameter commitment constraints.

