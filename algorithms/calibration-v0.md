# Calibration v0

Status: RC4 deterministic baseline.

The current RC4 implementation uses deterministic fixed intervals and threshold policies. Mainnet promotion requires signed calibration tables, ECE reports, and domain-specific model promotion gates linked from `model_card.v2` and `evaluation_report.v1`.
