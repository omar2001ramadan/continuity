# Drift Baseline v0

Status: RC4 deterministic profile.

Drift reports compare baseline and observation windows using fixed-point basis point values. Component scores for key, graph, action, cadence, claim, agent, and local drift are folded into the final drift score. Dormant high-value reactivation produces `dormant_reactivation` and `step_up`.

