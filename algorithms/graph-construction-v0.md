# Graph Construction v0

Status: RC4 deterministic profile.

RC4 uses `connected_components` as the only executable community profile. Graph edges are typed, directed, temporal, and weighted. Effective weight is computed from base weight, source quality, temporal decay, receipt status, and appeal/reversal multipliers, all encoded as integer basis points.

