# Reference Scorer v0

Deterministic integer-basis-point scorer implemented by `computeReferenceScoreV0`. It gates cryptographic failures before scoring, computes floor-rounded weighted terms, and returns insufficient evidence when coverage is below domain policy.
