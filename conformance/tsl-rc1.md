# TSL RC1 Conformance

Scope: Receipts, Merkle log, inclusion proof, checkpoint registry, and revocation registry.

Run:

```bash
npm test
```

Status is determined by schema validation, valid/invalid examples, deterministic test-vector canonical hashes, and the TypeScript verifier/algorithm tests.
